[project]
name = "artist-portfolio-montana"
version = "0.1.0"
description = "Artist portfolio website for an artist (Watercolour & Photography)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "Django>=5.0,<6.1",
    "Pillow>=10.0",
]
